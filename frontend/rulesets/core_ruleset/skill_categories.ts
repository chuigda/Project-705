import { SkillCategory } from '@app/core/ruleset'

const skillCategories: SkillCategory[] = [
   {
      ident: '@basic',
      name: '$sc_basic',
      description: '$sc_basic_desc'
   }
]

export default skillCategories
